# Flutter validation

Phase 2 messenger migration validation is expected to pass all repository gates:

- Dart formatting check
- `flutter analyze --fatal-infos`
- Flutter widget/unit tests
- Android debug APK build

The permanent CI workflow is read-only and does not mutate source files. Previous diagnostic output was temporary and has been replaced by this validation summary.
